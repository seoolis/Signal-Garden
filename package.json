{
  "name": "signal-garden",
  "private": true,
  "version": "3.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/server.mjs --root . --port 5173",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/server.mjs --root dist --port 4173",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
